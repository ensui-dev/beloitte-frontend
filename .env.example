# Set to "true" for development with mock data fallback (no backend needed)
VITE_MOCK_MODE=true

# Backend API base URL
VITE_API_URL=http://localhost:3001/api

# Bank identifier for this deployment (each bank gets its own instance)
VITE_BANK_ID=demo-bank-001
