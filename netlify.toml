[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_MOCK_MODE = "true"

# SPA routing - serve index.html for all non-file routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
