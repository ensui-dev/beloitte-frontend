/**
 * Re-export theme types from the schema.
 * Keeps imports clean: `import type { ThemeConfig } from "@/lib/theme/types"`
 */
export type { ThemeConfig, ThemeColors } from "@/lib/config/site-config-schema";
